import { boolean, integer, jsonb, pgTable, primaryKey, text, timestamp } from 'drizzle-orm/pg-core'

export const categories = pgTable('categories', { id: text('id').primaryKey(), name: text('name').notNull(), slug: text('slug').notNull().unique(), description: text('description'), createdAt: timestamp('created_at', { withTimezone: true }).defaultNow().notNull() })
export const tags = pgTable('tags', { id: text('id').primaryKey(), name: text('name').notNull(), slug: text('slug').notNull().unique(), createdAt: timestamp('created_at', { withTimezone: true }).defaultNow().notNull() })
export const posts = pgTable('posts', { id: text('id').primaryKey(), title: text('title').notNull(), slug: text('slug').notNull().unique(), excerpt: text('excerpt'), content: jsonb('content').notNull().default({}), coverImage: text('cover_image'), status: text('status').notNull().default('draft'), categoryId: text('category_id'), author: text('author').notNull().default('سروش'), createdAt: timestamp('created_at', { withTimezone: true }).defaultNow().notNull(), updatedAt: timestamp('updated_at', { withTimezone: true }).defaultNow().notNull(), publishedAt: timestamp('published_at', { withTimezone: true }), readingTime: integer('reading_time').notNull().default(1), seoTitle: text('seo_title'), seoDescription: text('seo_description'), isFeatured: boolean('is_featured').notNull().default(false) })
export const postTags = pgTable('post_tags', { postId: text('post_id').notNull(), tagId: text('tag_id').notNull() }, (table) => [primaryKey({ columns: [table.postId, table.tagId] })])
export const siteSettings = pgTable('site_settings', { id: text('id').primaryKey(), siteName: text('site_name').notNull(), heroText: text('hero_text').notNull(), bio: text('bio'), profileImage: text('profile_image'), socialLinks: jsonb('social_links').notNull().default({}), updatedAt: timestamp('updated_at', { withTimezone: true }).defaultNow().notNull() })
